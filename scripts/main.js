// basic cookie stuff
var cookies = 0;
var power = 1;

// helpers
var clicker = 0;
var grandma = 0;

function increment() { // click cookie make cookie
    cookies = cookies + power;
    counter.innerHTML = cookies;
}
